# ===String ===
my_name = "Nicolas"

# ver que tipo de variable es cada variable
print(my_name, "es una variable de tipo:", type(my_name))

# === int ===

my_age = 12
# Aqui el tipo de variable es int, pero si lo concatenamos contexto se vuelve string
print(type(my_age))
print(my_age, "es una variable de tipo:", type(my_name))

# === Boolean ===
is_single = True

print(type(is_single))

# NOTA: al utilizar la función "input()", siempre el valor obtenido sera de tipo "String"
